# 预训练方案 —— 最终决定

**日期**：2026-07-31 · **依据**：AGENT.md §13.44a / §13.45 · **状态**：已敲定

---

## 一句话

**目标函数用标准 `random` MAE，且只在监督训练会失败的任务上做预训练。**

在监督能正常工作的数据集上，**最优预训练方案仍然不如不预训练** —— 当前规模下预训练是
救场手段，不是通用先验。

---

## 1. 方案配置

```yaml
# 架构（已冻结，不是本次消融的变量）
arch: triaxial
tokenizer_mode: pac_interaction     # 强制、规范不变的 PAC token
pac_token_mode: measured
freq_mixer: attention               # 频率轴用普通 attention
band_pe: index                      # 见 §3 注意事项
spatial_pe: xyz
n_bands: 8
d_model: 128
depth: 6
n_heads: 4

# 预训练目标
pretrain_task: mae                  # 默认值
mask_mode: random                   # ← 本次消融的结论
mask_ratio: 0.5

# 关键超参
weight_decay: 0.00001               # 见 §3，这一项被量到影响 +0.05
lr: 0.0001
```

**使用条件**：仅当该任务的监督从零训练会崩溃（低标注 / 极端类别不平衡）时启用。
否则直接监督训练。

---

## 2. 证据

九个目标函数 × 三个数据集，seed 0。TUEV / Sleep-EDF 是 kappa，CHB-MIT 是 PR-AUC，
**只在列内比较**。

| 目标函数 | TUEV | CHB-MIT | Sleep-EDF |
|---|---|---|---|
| **无预训练（监督从零）** | **0.5688** (hz) / 0.5493 (index) | 0.0180 *（塌陷）* | **0.5732** |
| **`random` MAE ← 选定** | 0.5024 | **0.5767** | 0.5572 |
| `mixed` (cf_mixed) | 0.5164 | 0.5624 | — |
| `crossfreq` | 0.4531 | 0.5273 | — |
| `bandrand` | 0.4034 | 0.3319 | — |
| `pac_consistency`（随机负样本） | 0.5693 | 0.3685 | — |
| `pac_consistency`（确定性负样本） | **0.5879** | 0.3196 | 0.5278 |
| MAE + 0.3×consistency | 0.5027 | 0.4887 | — |
| MAE + 1.0×consistency | 0.5783 | *（取消）* | 0.5476 |

作业号：15106220、15107124-28、15123905-10、15150645-51。

### 判据（结果出来之前定死）

**minimax**：取三个数据集上 worst-case 最好的。之所以不用平均，是因为
§13.21 / §13.40-B / §13.44 三次都是"单数据集大赢 → 写进结论 → 第二个数据集翻车"。

| 目标函数 | TUEV | CHB-MIT | Sleep-EDF | worst |
|---|---|---|---|---|
| `pac_consistency`（确定性） | +0.0855 | −0.2571 | −0.0294 | **−0.2571** |
| MAE + 1.0×cons | +0.0759 | *(≤ −0.088)* | −0.0096 | **≤ −0.088** |
| MAE + 0.3×cons | +0.0003 | −0.0880 | — | **−0.0880** |
| `crossfreq` | −0.0493 | −0.0494 | — | **−0.0494** |
| `mixed` (cf_mixed) | +0.0140 | −0.0143 | — | **−0.0143** |

**无人超过 `random` MAE。** `cf_mixed`（本项目原本的核心方案）是最接近的落选者，
worst-case −0.0143 —— 平局，但从未赢过。

### 使用条件的依据

```
TUEV       random MAE 0.5024  <  纯监督 0.5688     −0.067
Sleep-EDF  random MAE 0.5572  <  纯监督 0.5732     −0.016
CHB-MIT    random MAE 0.5767  >  纯监督 0.0180     +0.559
```

两个监督能工作的数据集上，最优预训练方案都输给不预训练。唯一的胜利来自
CHB-MIT，而那里监督直接塌到基线正样本率（1% 正样本）。

---

## 3. 三条必须一起带走的注意事项

### (1) `weight_decay` 是真旋钮，不要沿用旧的 1e-4

TUEV 上从 `1e-4` 改成 `1e-5`（对齐监督协议）：

| | wd=1e-4 | wd=1e-5 | 差 |
|---|---|---|---|
| `random` MAE | 0.4505 | 0.5024 | **+0.052** |
| `mixed` | 0.4726 | 0.5164 | **+0.044** |

这个量级足以翻转结论。**预训练的 wd 必须和下游监督协议对齐。**
（CHB-MIT 保持 1e-4，因为那边监督是塌的，没有可对齐的参照。）

### (2) `band_pe: index` 保持不动，但理由变了

原理由是"与 `cf_mixed` 配合最好、Hz PE 会与目标函数的频率先验干扰"（§13.40-B）。
**该理由已作废** —— 六个 BandPE 对比（两数据集 × 两目标函数 × 两架构）方向全不一致。

**新理由：没有任何证据支持改动，改了就是白送一个自由度。**

### (3) 所有结论都来自单数据集小规模预训练

15–30 epoch、单语料。**大规模多语料下是否改变，未知且未测。**
若大集群的价值主张是"多语料大规模能翻盘"，必须先用一个小规模多语料对照验证
这个假设，不要直接投产。

---

## 4. 被排除的方案，以及各自留下的可用信息

### `cf_mixed`（原方案）
worst-case −0.0143。平局，从未赢。**不再作为 novelty 主张。**

### `crossfreq`（PAC 形状遮盖）
两个数据集都输 `random` 约 0.049。

**但它稳定赢 `bandrand`**（TUEV +0.0497，CHB-MIT +0.1954），而两者遮的整带数量完全相同、
只是位置不同 —— **遮盖方向确实携带信息**。准确表述：

> 整带遮盖不如 token 级随机遮盖；而在整带遮盖内部，高频方向是最好的那一种。

### `bandrand`
最差。作为机制对照有用，不作为方案。

### `pac_consistency`（本轮自创）
判别 token 是否由真实优选相位构造。设计动机是我们自己的消融
（§13.43-J1：优选相位 +0.0667，强度加权 +0.0092），指出 MAE 只监督了 token 的幅值一半。

**TUEV 上 0.5879，是整个项目第一个打赢监督从零的预训练目标**（+0.019）。
但 CHB-MIT 和 Sleep-EDF 都输。三取一，不予采纳。

值得记录的两点：
- **伪影已排除**：确定性负样本（0.5879）优于随机负样本（0.5693），说明判别器学的是
  相位归属而非随机性签名。那个数是真的，只是不复现。
- 代码保留在 `models/pretrain.py`（`pretrain_task=pac_consistency`），默认不启用。

### MAE + consistency 组合
权重是真旋钮（TUEV：0.3 → 0.5027，1.0 → 0.5783），但 CHB-MIT 上加入 consistency 项
就是有害的（w=0.3 已掉 0.088）。淘汰。

---

## 5. 一条能省未来算力的硬约束

**直接回归 `∠Z[i,j]` 在原理上不可能。**

measured token 是**规范不变**的（§13.43-C：平移某频带的相位零点，token 精确不变，
实测 8.3e-07），而 `∠Z[i,j]` 是**规范协变**的。不变的表示无法确定协变的量。

所以只有**规范不变**的目标是 well-posed：
- 二元一致性标签（`pac_consistency` 用的）
- `|Z[i,j]|`（耦合强度）
- 共享源频带的相位**差**（`∠Z[i,j] − ∠Z[i,j']`）

这条约束是这个架构独有的（别人没有不变性，不会遇到），它在花任何算力之前
就排除了大部分显而易见的相位预测设计。

---

## 6. 诊断记录：难度不预测迁移

各目标函数的最终重建损失（TUEV）：

| 目标函数 | recon 首 | recon 末 | 降幅 | 下游 |
|---|---|---|---|---|
| `random` | 0.1934 | 0.0267 | 7.2× | 0.5024 |
| `mixed` | 0.3725 | 0.0434 | 8.6× | 0.5164 |
| **`crossfreq`** | 0.3767 | **0.0193** | **19.5×** | 0.4531 |
| `bandrand` | 0.3548 | 0.0500 | 7.1× | 0.4034 |

`crossfreq` 把自己的 pretext 解得最彻底、迁移最差 —— 符合 Phase-Swap 报告的
"任务太易则学不到东西"失效模式（PAC tokenizer 显式做了 low→high 路由，把这个任务
变简单了；§13.40-D 当时是 0.063，现在 0.0193）。

**但 `bandrand` 是最难的而迁移最差。难度与迁移不单调，不要拿它当设计规则。**

---

## 7. 一句话交接

用 `random` MAE，`weight_decay` 对齐下游，`band_pe: index` 不动，
**只在监督会崩的任务上预训练**。其余目标函数全部试过并淘汰，
不要在没有新的大规模证据之前重开。
