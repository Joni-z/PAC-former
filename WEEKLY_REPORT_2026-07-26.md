# 本周工作详细记录(2026-07-19 会议 → 2026-07-26)

本文档覆盖上次组会(2026-07-19)之后到现在的全部工作,包含所有实验数据、所有 job id、以及每一次方向调整的原因。逐条对应 `AGENT.md` §13.17–§13.42。凡是**已作废(RETRACTED)**的结果都明确标出,不要引用。

---

## 0. 起点:上周汇报的状态

1. 关闭一条路:PAC-as-layer 在监督下三次证伪,根因是监督无动力学逼出跨频。
2. 打开一条路:crossfreq MAE 3/5 赢标准 MAE,CHB-MIT 从 0.53 救到 0.878 AUROC——SSL 基石第一个正面证据。
3. 修复:耦合计算从全局平均改成按 (通道, patch)。
4. 提出更强 novelty:parameter-free phase-steered mixer,TUAB/TUEV 最优。
5. 未决:phase_align 坏了;2×2 旗舰格子未跑;CHB-MIT phase 超时;全部单 seed。

---

## 1. 收尾:Sleep-EDF n_bands 混淆项 + PAC 算子线正式关闭(§13.17, §13.20)

**问题**:v1(n_bands=32)在 Sleep-EDF 上 coupling 赢 cotar +0.036(kappa 0.5354 vs 0.4996,3 seed 均值),但 v2(n_bands=8)这个优势消失甚至反转(coupling 0.531 vs cotar 0.572)。唯一没做过的对照:把 n_bands 恢复到 32,看优势是否回来。

**决定性测试结果(2026-07-22 完成,单 seed,jobs 14570741/742/743)**:

| mixer @ n_bands=32 | balanced_acc | f1_weighted | test_kappa | vs n_bands=8 |
|---|---|---|---|---|
| coupling | 0.6240 | 0.7150 | **0.5482** | 0.531 → 0.548(+0.017) |
| cotar | 0.6221 | 0.7135 | 0.5476 | 0.572 → 0.548(−0.024) |
| attention | 0.6348 | 0.7057 | 0.5378 | 0.511 → 0.538(+0.027) |

**结论:优势没有回来。** n_bands=32 下 coupling(0.5482)只是和 cotar(0.5476)打平(+0.0006),远不是 v1 的 +0.036。同时 `pac_scale` 在全部 6 层单调收敛到 ~0(v1 是唯一 pac_scale 存活的地方,这次也没能存活)。v1 的优势被证实是"band×band 全连接 vs cotar 的秩-1 压缩"这个架构效应,与 PAC 无关。

**PAC-operator 路线正式关闭**(按 §13.17 事先约定的停止规则)。

---

## 2. cf_mixed 目标函数的建立(§13.21)

**问题**:crossfreq(遮上半频带重建)在二分类上大赢,但在多分类(TUEV/Sleep-EDF)上输给标准随机 MAE——被诊断为"pretext 太具破坏性"。

**mask-shape 消融结果(11 个 job 全部完成)**:

| 数据集(指标) | crossfreq | random MAE | **cf_mixed** | cf_partial | cf_quarter |
|---|---|---|---|---|---|
| TUEV(κ,6类) | 0.271 ✗ | 0.356 | **0.385 ✓** | 0.312 | 0.278 |
| Sleep-EDF(κ,5类) | 0.451 ✗ | 0.483 | 0.474 | **0.497 ✓** | 0.457 |
| TUSZ(pr_auc,二分类) | 0.551 | 0.453 | **0.553** | 0.485 | 0.557 |
| TUSZ(auroc) | 0.836 | 0.809 | 0.828 | 0.800 | 0.830 |

(cf_partial = 上半区@50%密度;cf_quarter = 上四分之一@100%密度,两者都遮 25% 总量,只差形状。cf_mixed = 逐 batch 硬币决定 crossfreq/random,`mixed_p=0.5`,平均遮 50%。)

**结论:cf_mixed 在保住二分类大幅优势的同时,把多分类修好了**(TUEV 0.385 vs random 0.356,+0.029;TUSZ pr_auc 0.553 ≈ crossfreq 0.551 ≫ random 0.453)。这是"单一目标函数、两种任务体制都能用"的第一个证据,成为后续默认配方。

**后续确认(TUAB/CHB-MIT,jobs 14619136/137,完成)**:

| 数据集 | 指标 | crossfreq | random | **cf_mixed** |
|---|---|---|---|---|
| TUAB | pr_auc | 0.862 | 0.811 | 0.844 |
| TUAB | auroc | 0.857 | 0.809 | 0.845 |
| CHB-MIT | pr_auc | 0.393 | 0.136 | **0.438**(超过 crossfreq) |
| CHB-MIT | auroc | 0.878 | 0.743 | 0.866 |

cf_mixed 在这两个数据集也保住了大幅优势,一度被认为"在全部 5 个数据集上验证通过"(**这个说法后来在 §13.28 被更正为 4 胜 1 负**,见下)。

**caveat**:全部单 seed;Sleep-EDF 上 cf_mixed(0.474)略低于 random(0.483),cf_partial 才是 Sleep 赢家,所以 cf_mixed 不是严格全面占优。

---

## 3. Pre-pretrain 方案定型 + 架构问题讨论(§13.22–13.27)

### 3.1 方案定型(§13.22)

- **卖点**:把"跨频重建"这个物理先验编码进目标函数而不是算子——"先验放目标函数、不放算子"(§13.18 命题的具体应用)。
- **冻结的架构决定**:tri-axial backbone、三轴全部用 attention(耦合算子已关闭)、xyz 电极坐标 + 中心频率 BandPE、cf_mixed 目标函数。

### 3.2 架构问题(§13.23)

用户在 pretrain 关口提出:"如果 novelty 是训练目标,那模型怎么办,不能只用最基础的 transformer。"

结论:这不矛盾。§13.18 否定的是"架构里一个可被绕过的可选加法分支"(如 `pac_scale·coupling` 挨着自由 attention),不是否定"定义 token 是什么、谁和谁能 attend、坐标系是什么"这类结构性先验。**设计规则:把每个 EEG 先验都编码成坐标系/相对位置编码,而不是自由 attention 旁边的加法门。**

**发现的差距(诚实记录)**:`SpatialPE` 当时其实只是一个学习式序号嵌入,不是真正的 xyz 坐标——"montage-agnostic"是愿景不是实现。列为架构头号待办。

### 3.3 baseline 对齐审计(§13.24, §13.26)

| 数据集 | 蒙太奇 | 采样率 | vs BIOT | 是否可比 |
|---|---|---|---|---|
| TUAB | 16通道双极(顺序一致) | 200Hz | **与 BIOT 逐字节一致** | ✅ 可比 |
| TUEV | 16通道双极 | 250Hz(与 BIOT 一致) | 逐段移植 | ✅ 可比 |
| TUSZ | 复用 TUAB 蒙太奇 | 200Hz | BIOT 无此数据集处理器 | ⚠️ 不适用 |
| CHB-MIT | 16通道双极(原生) | **200Hz(重采样)** | **BIOT 原生 256Hz,分歧** | ❌ 不可比 |

结论:TUAB/TUEV 是干净的 BIOT 对齐锚点;CHB-MIT 只能内部用(重采样率不一致)。

### 3.4 全量微调路径(§13.27)

给 `pretrain.py` Phase 2 加了 finetune 模式(此前只有 linear probe)。**当时误把 `_ft` 结果当成"vs BIOT 的头条数字",同日被自我更正**:那只是"单数据集自预训练→微调",不是 foundation model,不能拿去和 BIOT 的几千小时预训练比。

### 3.5 xyz SpatialPE 落地(§13.25)

新建 `models/montage.py`,从 mne `standard_1020` 提取 10-20 坐标,双极通道编码成两端点 xyz 拼接(6维)。`SpatialPE` 改造为可选坐标输入,默认行为不变(向后兼容)。CPU 冒烟测试全绿。

---

## 4. Pre-pretrain 证据台账(§13.28)

### 4.1 Link 1 — cf_mixed vs random MAE(单数据集预训练+linear probe,单 seed)

| 数据集 | 指标 | cf_mixed | random MAE | 结论 |
|---|---|---|---|---|
| TUAB | pr_auc | 0.8438 | 0.8107 | 赢 |
| TUEV | bal-acc/κ | 0.4939/0.3853 | 0.4718/0.3562 | 赢 |
| TUSZ | pr_auc | 0.5529 | 0.4531 | 赢 |
| CHB-MIT | pr_auc | 0.4383 | 0.1356 | 大赢 |
| **Sleep-EDF** | bal-acc/κ | **0.5755/0.4741** | **0.5864/0.4829** | **输** |

**更正:cf_mixed 不是"5/5 验证通过",是 4 胜 1 负。** Sleep-EDF 是反例。

### 4.2 band-locality 诊断(试图解释 Sleep-EDF 反例,`scripts/band_locality.py`,CPU job 14655505)

用逻辑回归测每个数据集"标签信息在哪个频段"(低半/高半/全部),4000 训练/4000 测试窗口:

| 数据集 | chance | 低半 bal-acc | 高半 bal-acc | 全部 | cf_mixed 输赢 |
|---|---|---|---|---|---|
| Sleep-EDF | 0.200 | 0.5804 | **0.5554** | 0.5695 | **输** |
| TUAB | 0.500 | 0.6663 | **0.4881** | 0.6408 | 赢 |
| TUEV | 0.167 | 0.5773 | 0.3282 | 0.5570 | 赢 |
| TUSZ | 0.500 | 0.6856 | **0.4582** | 0.6374 | 赢 |
| CHB-MIT | 0.500 | 0.7783 | **0.8172** | 0.8638 | 大赢 |

（二分类 AUROC:TUAB 低 0.7278/高 0.4982,TUSZ 低 0.7517/高 0.4454,CHB-MIT 低 0.8439/高 0.9100）

**假说"Sleep-EDF 高频没有标签信息"被证伪**:Sleep-EDF 高半频段信息量(0.5554)远超随机(0.200),关系是反的——cf_mixed 赢得最漂亮的 TUAB/TUSZ,高半频段反而在随机水平或以下。Sleep-EDF 反例**至今未获解释**(后来发现这批数字也受到 §13.31 的指标 bug 影响,反例本身是否成立都存疑)。

### 4.3 Link 2/3/4/5 — 当时状态

- **Link 2(xyz SpatialPE)**:此前的 `_ft` 跑法一次改了三个变量(finetune 模式、finetune_lr、xyz PE),涨分归因不清,列为"NO EVIDENCE YET"。
- **Link 3(频率轴)**:新增 `freq_mixer: none`(彻底切断跨频信息流,验证梯度严格为 0),列为"NO EVIDENCE YET"。
- **Link 4(vs BIOT)**:写好 `baseline_biot.py`,让 BIOT 跑在我们的 dataloader+eval 上,列为"NO EVIDENCE YET"。
- **Link 5(hz BandPE)**:新增 `band_pe: index/none` 开关,列为"NO EVIDENCE YET"。

这四条后来在 §13.30–§13.40 之间陆续跑出并反复修正。

---

## 5. 合并预训练机制搭建(§13.29)——后来被弃用但过程记录在案

在仓库里从零搭建了跨数据集合并预训练的 dataloader(`PooledPretrainDataset` + `build_pretrain_pool`):

| 成员 | 训练窗口数 |
|---|---|
| TUAB | 298,396 |
| TUEV | 75,768 |
| TUSZ | 230,477 |
| CHB-MIT | 307,628 |
| **合并** | **912,269** |

Sleep-EDF 因蒙太奇不同(2通道)+ cf_mixed 在其上失利,被排除。CPU 冒烟验证:同一套权重能同时处理 5 秒(合并语料裁剪长度)和 10 秒(TUAB 下游微调长度)输入。**此机制在 §13.33 被移出本集群、留给大集群使用,本集群未曾实际运行合并预训练。**

---

## 6. TUEV 灾难与修复(§13.30 作废 → §13.31 修复 → §13.32 审计)

### 6.1 §13.30 的原始结果(⚠️ 全部作废,不要引用)

当时得出"三个物理先验全部失败、我们从零训练输给 BIOT 0.095 kappa"的结论。**这批数字后来被发现是模型选点指标用错导致的假象,已作废重算。**

### 6.2 根因(§13.31)

`pretrain.py`/`train.py`/`baseline_biot.py` 三个 runner 统一用 `"auroc" if 二分类 else "balanced_accuracy"` 选最优 epoch。但 BIOT 官方多分类协议用的是 `cohen_kappa`(`run_multiclass_supervised.py:288`)。

**这不是无害偏差,而是专门坑我们自己的模型**:我们模型的验证 bal-acc 和 kappa 在不同 epoch 上**反着走**(epoch 4 的 bal-acc 最高 0.5783 但 kappa 只有 0.4134;epoch 13 的 kappa 最高 0.4825),而 BIOT 的两个指标同向变化,bal-acc 选点对它几乎无损。

**修复**:统一到 `eval.select_key(num_classes, cfg)`,二分类用 auroc,多分类用 cohen_kappa,三个 runner 全部导入同一实现。**§13.21/§13.28/§13.30 里所有 TUEV 行和 Sleep-EDF 行全部作废**,TUAB/TUSZ/CHB-MIT(二分类,一直用 auroc)不受影响。

### 6.3 §13.32 顺带审计

确认 BIOT loader 不做数据增广,所以我们从零训练用 `augmentations: []` 是对称匹配不是偏袒;顺带用 `init_from` 机制救回一个即将撞墙(12小时限时跑到10:50)的作业,省了约9小时重复预训练。

给 `scripts/collect_results.py` 加了自动隐藏机制——按 job id 判断是否在指标修复之前跑的多分类结果,自动标 `[STALE]` 隐藏,防止同一个 bug 再犯。

---

## 7. 范围收缩:预训练移出本集群(§13.33)

**用户指令(2026-07-23)**:"我不需要你做合并预训练……我们做预训练会放到一个更大的集群上面跑更多的数据集,你现在在排队等卡再做预训练太浪费时间了。"

**立即执行**:取消 13 个排队/在跑的预训练类作业(约 45 GPU-小时),保留 7 个不涉及我方预训练的作业(从零监督训练、BIOT 两种初始化、一个救援作业)。

**范围重新定义**:这个集群此后只负责"该拿哪个架构去预训练"这个问题——完全用**从零监督训练**回答,不需要任何预训练。cf_mixed 本身作为预训练问题,也一并移交大集群,**此后本集群不再产出任何 cf_mixed 的新证据**;此前 §13.21/§13.28/§13.30 里 TUEV/Sleep 的目标函数结果保持作废状态,不再补跑。

排上的 8 格架构消融矩阵(从零训练,20 epoch,对齐 BIOT 协议):完整三轴 / 去频率轴 / 去xyz坐标 / 去hz-BandPE,TUAB+TUEV 各一套,加 4 格 BIOT(从零+预训练,TUAB+TUEV)。

---

## 8. mixed_p 剂量曲线讨论(§13.34)——决定不在本集群调

`mixed_p=0.5` 的两端已经测过(`mixed_p=0` 就是 random 行,`mixed_p=1` 就是 crossfreq 行):

| PR-AUC | p=0(纯random) | p=0.5(cf_mixed) | p=1(纯crossfreq) |
|---|---|---|---|
| TUAB | 0.8107 | 0.8438 | **0.8615** |
| TUSZ | 0.4531 | **0.5529** | 0.5514 |
| CHB-MIT | 0.1356 | **0.4383** | 0.3929 |

0→0.5 三个数据集全部大涨(稳);0.5→1 三个数据集意见不一致(无共同最优点)。**决定不在本集群调这个超参**(单数据集小规模调出来的最优值大概率不迁移到大集群的合并大规模预训练),但作为"剂量-反应曲线"这个机制证据,写进大集群任务清单。

---

## 9. BIOT 配方错误(§13.35 作废)+ 系统性审计(§13.36)

### 9.1 §13.35:第一版 BIOT 数字作废

Job 14657775(BIOT 加载 `EEG-PREST-16-channels.ckpt`,TUAB)一度显示 `bal_acc=0.7894 auroc=0.8702 pr_auc=0.8658`,**低于**我们参数量减半的模型——这个对比不成立。

**根因**:配置里给 BIOT 用了 `lr=1e-4, batch_size=32`(为了"和我们对称"),但 BIOT 官方默认是 `lr=1e-3, batch_size=512`。TUAB 约 30 万训练窗口,bs=32 意味着**每 epoch 9325 步,是 BIOT 设计值(583步)的 16 倍**——它在第一个 epoch 就训过头了,验证曲线在 epoch 0 见顶后单调下滑。

**修复**:把"BIOT 当目标数"和"架构对架构"两个用途拆开——前者用 BIOT 自己的原生配方(lr 1e-3, bs 512),后者保持对称协议不变。

### 9.2 §13.36:验证曲线采样率审计

同样的"结果反常回头查"套路,这次抓到第三种问题:**TUAB 每 epoch 验证一次,但一个 epoch 就是 9324 步,真正的最优点落在 epoch 内部,±9324 步的采样分辨率完全抓不到它**。不止 BIOT,我们自己模型的招牌 TUAB 数字峰值也落在 epoch 0。

**修复**:三个 runner 全部加 `eval_every_steps`(epoch 中途验证,不改变任何训练动态,只是采样更密),默认关闭不影响历史结果。同时把 20 epoch 缩到 5 epoch(峰值都在最早期),验证点从 20 个(18 个在无用区)变成 31 个(全部覆盖峰值区),训练量反而降到约 1/3。

**教训(三次同型):** §13.31 选点指标错、§13.35 学习率错、§13.36 验证采样率错,**三次都在最终 test 数字里看不出来,三次都在逐 epoch 曲线里一眼可见**。定下规矩:任何数字被使用前先看验证曲线;任何 baseline 若峰值落在 epoch 0,默认视为配置有问题。

---

## 10. 架构决策结果——TUAB + TUEV 完整(§13.37, §13.38)

修复分辨率问题后的最终结果(从零监督训练,单 seed):

**TUAB(AUROC / PR-AUC,完整三轴为基准)**

| 变体 | 移除了什么 | AUROC | PR-AUC |
|---|---|---|---|
| 完整三轴 | — | 0.8738 | 0.8718 |
| freqnone | 频率轴 | 0.8603 | 0.8619 |
| idxpe | xyz坐标 | 0.8737 | 0.8739 |
| **bandidx** | hz-BandPE | **0.8775** | **0.8809** |

**TUEV(kappa)**

| 变体 | kappa |
|---|---|
| 完整三轴 | 0.4544 |
| freqnone | 0.3761 |
| idxpe | 0.3743 |
| bandidx | **0.4679** |

**架构对架构(我们 vs BIOT,均从零训练,同一流水线)**

| | TUAB AUROC | TUAB PR-AUC | TUEV kappa |
|---|---|---|---|
| 我们完整三轴 | 0.8738 | 0.8718 | 0.4544 |
| 我们最优变体(bandidx) | 0.8775 | 0.8809 | 0.4679 |
| BIOT backbone(从零,对称协议) | 0.8780 | 0.8773 | 0.4449 |

**三个组件的判定(两数据集都支持)**:
1. **频率轴——保留。** 去掉后 TUAB 掉 0.0135 AUROC,TUEV 掉 0.078 kappa(大幅)。今天最硬的正面架构证据。
2. **xyz SpatialPE——保留。** TUAB 上无差,TUEV 上去掉掉 0.08(真实下降)。
3. **hz-BandPE——去掉更好(★后被 §13.39/§13.40 证明是"regime-dependent",不是普适结论,见下)。** bandidx 在 TUAB 和 TUEV 都是最优变体。

**BIOT 原生配方参照数(§13.38,清除 epoch-0 假象后)**:TUAB AUROC 0.8811,TUEV kappa 0.4125。**架构对架构从零打平**(TUAB 我们 PR-AUC 略领先,TUEV 我们领先 0.023)。

**大集群交接清单当时的结论**:backbone = 三轴,band_pe 换成 index,频率轴+xyz PE 保留。**这个"hz-BandPE 换成 index"的结论此后被证明不是普适的,见下一节。**

---

## 11. Q2:架构是否适配 cf_mixed?——CHB-MIT 替代关系发现(§13.39)

单数据集(CHB-MIT)15 epoch 预训练 → 微调,5 格:

| 配置 | PR-AUC | AUROC |
|---|---|---|
| random MAE + BandPE index(两个先验都没有) | 0.1575 | 0.7176 |
| **cf_mixed + BandPE index(频率轴在)** | **0.5472** | 0.9150 |
| random MAE + BandPE hz | 0.5466 | 0.9007 |
| cf_mixed + BandPE hz | 0.4081 | 0.8727 |
| cf_mixed + BandPE index + freqnone(无频率轴) | 0.4162 | 0.8506 |

**发现 1——cf_mixed 在全量微调下依然强力有效(此前只在 linear probe 下验证过)**:index 架构上,cf_mixed 把 PR-AUC 从 0.158 拉到 0.547(3.5倍)。补上了长期悬而未决的一个缺口。

**发现 2——cf_mixed 和 hz-BandPE 是替代品,不是叠加品**:任一单独存在都能到 ~0.55(cfm+index 0.547;rand+hz 0.547);两个都没有塌到 0.158;**两个都有反而干扰,降到 0.408**。二者都在注入"频率意识",一个走目标函数、一个走架构,叠加会打架。

**发现 3——推翻 §13.37 的"drop hz-BandPE"普适结论**:那是在监督从零训练下测的。在预训练体制下,这是 regime-dependent 的:配 random MAE 时 hz-BandPE 巨幅有用(0.158→0.547);配 cf_mixed 时它是冗余/有害的。**对最终要跑的 cf_mixed 而言,index 仍然正确**,因为目标函数已经承担了这个先验。

**发现 4——机制:cf_mixed 并非完全依赖频率轴生效**:预测是去掉频率轴后 cf_mixed 会塌回 0.16 附近的基线,但实际是 0.416,只比 0.547 少了 0.13。频率轴对 cf_mixed 有贡献(支持保留它),但目标函数似乎也通过共享的时间/空间表征起作用,不是单纯依赖跨频路径。

**caveat**:单数据集单 seed,CHB-MIT 1% 正样本 PR-AUC 抖动大,但 AUROC 方向一致且头条差距达 3.5 倍,超出噪声范围。

---

## 12. 候选结构决选 + CBraMod baseline + TUSZ 补全 + 干净机制测试(§13.40)

### 12.1 候选结构(TUEV kappa)

| 结构 | kappa |
|---|---|
| **bandidx(当前:三轴,attn频率,index BandPE,xyz PE,d128/depth6/8频带)** | **0.4679** |
| 完整三轴(hz BandPE) | 0.4544 |
| n_bands=16 | 0.4435 |
| d_model=192(更宽) | 0.4181 |
| freq_mixer=coupling(显式PAC算子) | 0.4175 |
| depth=8(更深) | 0.3632 |

**没有任何候选打赢当前简单结构**——coupling 又一次输给 attention(再次印证 §13.18/§13.20),更深/更宽/更多频带全部更差。**决定:大集群预训练用当前这个简单结构。**

### 12.2 CBraMod baseline(它自己的 µV/100 归一化,我们的划分+评测,权重 strict 加载 4.88M 参数)

| | TUAB PR-AUC | TUEV kappa |
|---|---|---|
| 我们 bandidx(从零) | 0.8809 | 0.4679 |
| BIOT 从零 | 0.8773 | 0.4449 |
| BIOT 预训练(原生) | 0.8809 | 0.4125 |
| CBraMod 从零 | 0.8635 | **0.5017** |
| CBraMod 预训练 | 0.8815 | 0.4436 |

**诚实说明**:TUEV 上 CBraMod 从零(0.5017)是全场最高,超过我们(0.4679)。它的 criss-cross backbone 在 TUEV 上确实强。另外 BIOT 和 CBraMod 在 TUEV 上都是"预训练反而不如从零"(单 seed,可能是 250→200 重采样带来的分布偏移)。

### 12.3 TUSZ 补全(Q1,PR-AUC,9%正样本)——发现跨数据集不一致

| 变体 | PR-AUC |
|---|---|
| 完整三轴 | **0.6049** |
| freqnone | 0.5947 |
| bandidx | 0.5871 |
| idxpe | 0.5658 |

**TUSZ 上完整版最优,去掉任何东西都掉分。hz-BandPE 在这里是有用的**(bandidx 掉 0.018),和 TUAB/TUEV 上"去掉更好"正好相反。**§13.37 的"drop hz-BandPE"因此不是跨数据集稳健的结论。**

三数据集组件汇总(移除该组件后指标变化):

| 组件 | TUAB(AUROC) | TUEV(kappa) | TUSZ(PR-AUC) | 判定 |
|---|---|---|---|---|
| 频率轴 | −0.014 | −0.078 | −0.010 | **保留**(到处都掉分) |
| xyz SpatialPE | ~0 | −0.080 | −0.039 | **保留**(TUEV/TUSZ有用,TUAB无害) |
| hz-BandPE | +0.006 | +0.014 | −0.018 | **有争议**——数据集和目标函数都相关 |

hz-BandPE 的矛盾由 §13.39 解释:它是目标函数承载的频率先验的替代品,有没有用取决于先验是否已从别处(cf_mixed)注入。

### 12.4 干净机制测试(纯 crossfreq,非混合;CHB-MIT PR-AUC)——挑战"强迫 PAC 耦合"这个叙事

| | PR-AUC | 预训练末期recon损失 |
|---|---|---|
| crossfreq × 频率轴 开 | 0.3457 | 0.063(任务可解) |
| crossfreq × 频率轴 关 | 0.3723 | 0.855(任务不可解) |

去掉频率轴后跨频重建根本做不了(recon 从 0.063 恶化到 0.855),但下游效果**没有变差**(0.372 vs 0.346,反而略高)。**说明 crossfreq 的下游收益不是来自"真的从低频重建出高频",即不是来自学到的低→高 PAC 路由。** 收益更可能来自"遮蔽分布本身"(藏掉高频→逼模型把低频编码好),而不是我们设计时假定的跨频预测机制。这个发现**没有削弱 cf_mixed 的有效性**,但动摇了一直用来解释它为什么有效的机制故事。

**这一节末尾给大集群的定案**:backbone 用当前 bandidx 配置(index BandPE,因为大集群跑 cf_mixed,先验由目标函数承担);频率轴+xyz PE 保留;没有容量/算子变体值得用。同时标记了一个待解决的科学问题:cf_mixed 到底为什么有效(机制测试和设计初衷矛盾)。

---

## 13. 文献撞车 + 机制彻底翻案(§13.41)

用户自行检索近期文献,发现严重撞车,逐条属实:

- **Phase-swap**(Lemkhenter & Favaro 2020, arXiv:2009.07664)——PAC 明确作为 SSL 目标函数,直接的在先证据。
- **MFM**(ICLR 2023, arXiv:2206.07706)——低通/高通频谱遮蔽+预测缺失频率。**crossfreq 在形式上就是 MFM 的一个实例**,而且 MFM 早就报告过我们 §13.40-D 的发现(高频结构和低频统计量都有用)。
- SpecMoE(2026-03)、TFM-Tokenizer(ICLR 2026, arXiv:2502.16060,频带×时间遮蔽 = cf_mixed)、band rejection/prediction(Jayalath 2024)、BandVQ(2026-05,按频带做 VQ tokenizer,占了"频带作为一等token"这个卖点)。

**结论:目标函数的 novelty(频谱遮蔽)和架构的 novelty(频带作token)同时被占。PAC-as-objective 这条线撑不起一篇论文。**

### 13.1 机制判决(CHB-MIT PR-AUC,纯目标函数,同架构,同遮蔽预算)

| 目标 | PR-AUC | 预训练末期recon |
|---|---|---|
| lowfreq(遮下半频谱) | **0.3669** | 0.106 |
| crossfreq(遮上半频谱) | 0.3457 | 0.063 |
| bandrand(遮同样数量的散落频带) | **0.1641** | 0.038 |
| random MAE(基线) | 0.158 | — |

**两个结论**:
1. **方向无关**——遮低频和遮高频一样好(甚至略好)。"低频相位→高频幅度"这个方向性 PAC 叙事经验上死了,和 MFM 的结论一致。
2. **连续性才是有效成分**——bandrand 遮同样数量的整频带但打散,结果塌回 random 基线。连续频谱缺口逼模型跨谱**外推**;散落频带可以从邻带**内插**(最容易的 pretext,recon 最低 0.038,迁移最差)。

### 13.2 我们的目标函数迁移到 CBraMod backbone(CHB-MIT)

| | PR-AUC | AUROC |
|---|---|---|
| CBraMod backbone + 我们的频谱目标 | **0.6087** | 0.9412 |
| CBraMod backbone + 它原生的随机token遮蔽 | 0.5585 | 0.9282 |

**+0.05 PR-AUC,在别人的 SOTA backbone 上**;而且 0.6087 高于我们自己 backbone 的 0.5472——**他们的骨干更强,我们的目标函数还能再提升它。这是唯一 backbone-agnostic 的结果。**

### 13.3 领域现状(两篇 2026 大规模评测)

- Liu et al.(arXiv:2601.17883,12模型×13数据集):从零训练的专家模型依然有竞争力、有时直接打赢 EEG foundation model;模型放大不带来更好泛化;**明确写了没有研究根因**。
- NeuroAtlas(arXiv:2605.14698):EEG **专用** FM 打不过**通用**时间序列 FM(既没有 EEG 架构也没在 EEG 上预训练过);同样没做根因分析。
- 我们自己独立复现了这个现象:TUAB 上 BIOT 预训练(0.8811)vs 从零(0.8780)几乎无差;TUEV 上 BIOT(0.4125 vs 0.4449)和 CBraMod(0.4436 vs 0.5017)**预训练都不如从零**。

---

## 14. 新方向定案:"先验不叠加"(Priors Don't Stack)(§13.42)

**主张(未被占用的空白点)**:一个领域先验是**替代品,不是叠加品**。§13.39 那组 2×2 就是机制解释:频率先验从目标函数注入(cf_mixed+index,0.547)或从架构注入(random+hz,0.547)都能拿到完整收益;都不注入就塌(0.158);**都注入反而互相干扰(0.408)**。这正好解释了 NeuroAtlas 的发现:EEG 专用模型重复编码同一个先验、付出代价,通用模型只注入一次或零次反而不吃亏。

**建设性推论**:既然先验只能花一次,就该花在**目标函数**上——因为它 backbone 可迁移(CBraMod +0.05 的证据),架构先验做不到这一点。目标模型 = 强通用 backbone + 恰好一个由目标函数承载的先验。

**定位**:MFM/TFM-Tokenizer/BandVQ 都是"再加一个先验";这篇论证"叠加先验本身是问题"。最近的邻居是音频 SSL 里的架构-目标**对齐**研究(arXiv:2607.00387),但"干扰/替代"这个主张未被提出过,EEG 领域里更是空白。

**风险与关卡**:整个方向压在一个**单数据集(CHB-MIT)单 seed** 的 2×2 上,PR-AUC 在 1% 正样本上本身就抖,两个"有先验"格子都落在 0.547 可能有巧合成分。**在复现之前不再往下建任何东西。**

**复现设计(按用户 2026-07-25 指示,不做多种子,改做跨数据集)**:配置由 `scripts/gen_substitution_matrix.py` 生成,与种子0原格逐行一致(仅差 seed/名字)。排上:TUEV 2×2 + TUSZ 2×2,共 8 个作业,取消了原计划的 16 个多种子作业。

三个独立数据集一致,能排除的是"数据集特异的假象",比同一数据集三个种子更能证明效应真实存在;但代价是**没有误差棒**,小差距(约 0.03 PR-AUC 以内)读不出方向。判据:每个数据集上,{只从目标函数注入}≈{只从架构注入}≫{都不注入},且{都注入}低于两者中较高的那个。**四格若挤在一起、或"都注入"格没被压低,替代关系不成立,方向需要再换。**

---

## 15. 写文档时的实时状态(2026-07-26)

替代关系复现矩阵(8 个作业:TUEV 2×2 + TUSZ 2×2,均 seed 0):

| job | 配置 | 状态 |
|---|---|---|
| 14787412 | sub_tuev_rand_s0 | ✅ 完成,kappa=0.4054 |
| 14787413 | sub_tuev_cfm_s0 | 🟢 运行中 |
| 14787414 | sub_tuev_randhz_s0 | 🟢 运行中 |
| 14787415 | sub_tuev_cfmhz_s0 | ⏳ 排队 |
| 14787424 | sub_tusz_rand_s0 | ⏳ 排队 |
| 14787425 | sub_tusz_cfm_s0 | ⏳ 排队 |
| 14787426 | sub_tusz_randhz_s0 | ⏳ 排队 |
| 14787427 | sub_tusz_cfmhz_s0 | ⏳ 排队 |

TUEV 的"两个先验都没有"这一格(rand_idx,kappa=0.4054)已经先跑出来,还差另外三格才能读出替代关系是否在 TUEV 上复现。TUSZ 一套还没开始。

---

## 16. 总结:现在站得住的、已作废的、悬而未决的

**站得住(有跨数据集/跨骨干证据)**:
- 频率轴作为架构组件在 TUAB/TUEV/TUSZ 上都有正向贡献,该保留。
- xyz SpatialPE 在 TUEV/TUSZ 上有用、TUAB 上无害,该保留。
- cf_mixed 在 linear probe 和全量微调下都稳定大幅优于标准 random MAE(二分类,3个数据集直接验证:TUAB/TUSZ/CHB-MIT)。
- 我们的频谱目标函数能提升 CBraMod 的 backbone,是唯一 backbone-agnostic 的正面结果。
- 频率先验的"替代关系"(目标函数或架构二选一,叠加会干扰)——**单数据集单seed,正在复现中**。

**已作废,不要引用**:
- §13.30 全部 TUEV 数字(选点指标错误)。
- §13.35 第一版 BIOT TUAB 数字(学习率配错)。
- 早期"cf_mixed 5/5 数据集验证通过"的说法(实为4胜1负)。
- "PAC 强迫低频→高频耦合"这个机制叙事(被自己的 lowfreq/bandrand 实验和 MFM 的在先文献同时否定)。
- hz-BandPE"该去掉"作为普适结论(实为 regime-dependent,TUSZ 上该保留)。

**悬而未决**:
- 替代关系的跨数据集复现结果(本文档写作时进行中)。
- Sleep-EDF 反例从未被正确解释过(band-locality 假说已证伪,而且原始数字还受过指标 bug 影响,反例本身是否成立都不确定)。
- cf_mixed 真正的作用机制(遮蔽分布本身 vs 跨频预测,尚无定论)。
- 大集群的合并预训练本身尚未开始运行——本集群交付的是架构决定和 baseline 参照数,不是 foundation model 本身。
