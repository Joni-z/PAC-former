# OURS from scratch (state 1), matched cell-for-cell to configs/biot_tuab.yaml so
# the ONLY difference vs the BIOT --init scratch run is the backbone:
# same data/splits/eval, epochs=20, lr=1e-4, NO augmentation, no pretraining.
# This is the architecture-vs-architecture comparison, valid before any pretrain.
mixer: attention
arch: triaxial
freq_mixer: attention
spatial_pe: index
dataset: tuab
data_root: /scratch/zz5070/PAC-former/tuh_eeg/v3.0.1/edf/processed
n_channels: 16
seq_len: 2000
sample_rate: 200
sampling_rate: 200
num_classes: 2
n_bands: 8
d_model: 128
depth: 6
dropout: 0.2
kernel_size: 201
patch_len: 200
n_heads: 4
batch_size: 32
num_workers: 8
seed: 0
device: cuda
epochs: 5              # every model peaks in epoch 0-1 on TUAB (13.36); 5 covers well past it
eval_every_steps: 1500   # 9,324 steps/epoch at bs32 -> ~6 validations per epoch (13.36)
eval_every: 1
patience: 0           # early stop is epoch-granular and meaningless at 5 epochs
lr: 0.0001
weight_decay: 0.00001
augmentations: []
wandb_run_name: ours-scratch-tuab-idxpe
